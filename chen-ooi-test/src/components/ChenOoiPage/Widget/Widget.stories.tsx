import { storyKit, Meta } from '@wix/yoshi-flow-editor/storybook';

import Settings from './index';

export const { Story: BasicRender } = storyKit.getSettings(Settings);

export default {
  title: 'Widget/ChenOoiPage',
} as Meta;
